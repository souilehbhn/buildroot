config BR2_PACKAGE_G2O
	bool "g2o"
  depends on BR2_PACKAGE_EIGEN
  help
    g2o is an open-source C++ framework for optimizing graph-based
    nonlinear error functions.
    https://github.com/RainerKuemmerle/g2o

comment "g2o depends on eigen"
	depends on !BR2_PACKAGE_EIGEN
